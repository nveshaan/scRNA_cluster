[project]
name = "scrna-cluster"
version = "0.1.0"
description = "clustering scRNA-seq using ML & DL"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.4.4",
    "scanpy>=1.12.1",
    "scvi-tools>=1.4.2",
]
